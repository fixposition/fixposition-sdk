// https://aka.ms/devcontainer.json
{
    "dockerComposeFile": "../../docker/docker-compose.yaml",
    "shutdownAction": "stopCompose",

    "name": "Fixposition SDK (no ROS)", "service": "deb-dev", "runServices": [ "deb-dev" ],

    // Use this user in the container, re-map IDs to local user automatically
    "remoteUser": "fpsdk",
    "updateRemoteUserUID": true,

    // compare .services.deb-dev.volumes in ../../docker/docker-compose.yaml
    "workspaceFolder": "/home/fpsdk/fixposition_utility",

    // Deliberately empty, see fpsdk.code-workspace instead
    "customizations": {}
}
